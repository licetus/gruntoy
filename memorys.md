# memorys.md —— 本工作空间的第一条规则

> **本文件的用途（先读这段）**
>
> 当单次对话的上下文已满、需要开启新对话时，**先读本文件**即可恢复项目进度与关键信息，
> 无需重新翻阅历史对话。每次完成一个任务对话后，把本次的关键信息（做了什么、
> 结论是什么、留下了什么、下一步是什么）追加进来，防止上下文丢失。
>
> **维护约定**
> - 追加而非重写：保留历史轨迹，新进展追加在「4. 任务进展日志」末尾。
> - 只记有长期价值的信息：结论、决策、约定、路径。不记临时搜索结果与工具报错。
> - 结构性变化（新增模块、改约定）要同步更新 §2 与 §3，不能只写日志。

---

## 1. 项目速览

| 项 | 内容 |
|---|---|
| 项目名 | **gruntoy v1.0** —— 毛绒电子宠物 · 体感子系统基线固件 |
| 技术栈 | ESP32 / Arduino C++17 / 平台无关本机测试（g++） |
| 交付位置 | `./gruntoy/` |
| 远端仓库 | **https://github.com/licetus/gruntoy**（public，`main`，SSH 远端） |
| 上游材料 | WorkBuddy 资料库「gruntoy设计方案」文件夹（23 个子节点） |
| 当前状态 | ✅ **v1.0 已交付并上线，M1 原型开发指南已就位**：6 套件 264 断言全绿、0 告警；远端 `main`；指南在 `gruntoy/docs/06-原型开发指南.md` |
| 阻塞项 | 无。下一步是**采购物料并动手做硬件**（非代码工作） |

**一句话定位**：把 PRD v1.1 的「呼吸 / 咕噜 / 心情值 / 摇尾分级 / 平滑过渡」五项机制，
落成一套可独立运行、可本机验证、可烧录调参的 ESP32 基线固件。

---

## 2. 关键结构与约定

### 2.1 目录结构

```
gruntoy/
├── gruntoy.ino         入口：setup() / loop()，唯一持有 Preferences 句柄
├── config.h            引脚、PWM 载波、编译期开关、NVS schema 版本
│                       ★ 唯一允许出现硬件常量的文件
├── params.h/.cpp       75 项参数 + 21 条校验规则 + NVS 持久化 + 三套预设
├── haptic.h/.cpp       体感引擎：呼吸 / 咕噜（门控调制）/ 裸测 / 扫频 / 提示震
├── mood.h/.cpp         心情值引擎：6 态状态机 / 闸门 / 呼吸触发链路
├── tail.h/.cpp         摇尾通道：档位 → 频率幅度 → ease-in-out 过渡
├── cli.h/.cpp          串口调试台（编译期可整体移除）
├── docs/               6 篇交付文档（01 设计意图 → 05 CHANGELOG → 06 原型开发指南）
├── README.md           快速上手：验证 / 接线 / 烧录 / 命令 / 调参流程
├── field-forms.html    M1 现场记录表 14 张（可打印，A4）
├── memorys.md          本文件的仓库副本（随仓库分发，改动后需同步并重新推送）
└── test/               Arduino 桩 + 断言框架 + 6 套件 + run_tests.sh
```

> **仓库根目录 = `gruntoy/` 目录**。`.gitignore` 排除 `test/build/`（宿主侧测试二进制与日志）。
> 远端 `origin` 走 **SSH**（`git@github.com:licetus/gruntoy.git`），**不要改成 HTTPS 带令牌的写法**。

### 2.2 必须遵守的工程约定

| # | 约定 | 原因 |
|---|---|---|
| 1 | **行为参数一律进 `params.cpp` 的 META 表，代码里不写体感数值** | FR-38；M1 调参靠串口，不靠重烧 |
| 2 | **`tick()` / `poll()` 路径中禁止 `delay()`** | 本机测试靠假时钟步进推进，`delay()` 会让状态机停摆 |
| 3 | **状态切换必经 `enterState()`** | 保证 `m_stateStart` 与副作用同步 |
| 4 | **改 `enum ParamId` 必须同步改 META 表，顺序严格一致** | `test_main` 会拦截错位 |
| 5 | **增删参数必须 `PARAMS_NVS_SCHEMA + 1`** | 否则旧 blob 被按新布局误读 |
| 6 | **新校验规则必须同时改 `validate()` 与 `ruleText()`** | CLI 与文档都引用 `ruleText()`，不另写文案 |
| 7 | **`haptic` 不认识「心情值」，`tail` 不碰机构选型** | 保持模块可单独测试、不越界 |
| 8 | **`validateSet()` 不钳制、`set()` 钳制** | 前者回答「这值能不能用」，后者用于加载 NVS |

### 2.3 两个编译期开关（`#ifndef` 包裹，可命令行覆盖）

```bash
-DENABLE_DEBUG_CHANNEL=1 -DENABLE_TAIL_OUTPUT=0   # 调试配置（默认）
-DENABLE_DEBUG_CHANNEL=0 -DENABLE_TAIL_OUTPUT=1   # 量产配置（CLI 整体移除）
```

---

## 3. 关键设计决策（勿重复讨论）

| 决策 | 结论与理由 |
|---|---|
| 呼吸谷底占空比 | **必须为 0**，由 `C-18` 强制。低占空比区有机械嗡鸣，听感是电流声而非呼吸 |
| 咕噜驱动频率 | 通过**门控调制**落地（把平均占空比在 f 上切成通/断脉冲）。否则「扫频找猫感区间」无物理差异 |
| `purr_tier_d3` 平台 | `b3~b4` 段刻意保持 D3，不外推。避免新增参数，且高段再拉长收益递减（OP-02） |
| 降落/冷却期抚摸 | **不计分但必须给反馈**。不给反馈会被判定「玩具失灵」；降落给柔震（L1），冷却给更重拒绝震（L2） |
| 首次抚摸 | **不做防抖判定**（`m_everStroked`）。否则开箱第一下被过滤，用户不会摸第二下 |
| 呼吸链路顺序 | `maybeTriggerBreath()` 放在**咕噜判定之后**，避免「先起呼吸随即被咕噜抢占」的无效启停 |
| 咕噜两道闸门 | `P-30` 轮次间隔 + `P-31` 累计上限，防止玩具「停不下来」 |
| 摇尾默认不输出 | `ENABLE_TAIL_OUTPUT=0`。机构选型属另一份执行清单，本基线不越界 |
| 云端参数层 | 只留接口不启用。关键约束：**拉取失败必须保持本地值，禁止回落硬编码** |
| 越界值处理 | **拒绝而非静默钳制**。静默钳制会让 `t gate 99` 看似成功实际夹到 60Hz，导致扫频记录不可信 |

---

## 4. 任务进展日志

### 2026-09-16 · 任务一：gruntoy v1.0 基线固件交付 ✅ 已完成

**任务来源**：用户提供资料库链接
`https://www.workbuddy.cn/space/d/YTNAelQgtZdogVHh8Zq8S2`（文件夹「gruntoy设计方案」，23 个子节点），
要求「通读并理解所提供的全部文档，梳理设计意图、核心目标与关键结构，在此基础上提炼出
功能完整、可独立运行且表述清晰的 1.0 基础版本，并保存到 gruntoy 文件夹中」。

**执行过程**

1. **材料通读**：经 `资料库` skill 鉴权（`connect_open_platform` 换票，token 不落地），
   导出 6 篇文档 + 下载 13 个网盘源码文件至 `.workbuddy/tmp/gruntoy_src/`。
   处理要点：PRD 达 538KB / 18035 行，无法一次读完，改用
   `strip.py`（剥 HTML 组件）+ `mdize.py`（转 Markdown）+ `fixh.py`（合并标题断行），
   再按 `grep -n '^#'` 定位附录 C（1299~1431 行）、§7.9（547~612 行）、§9.5/9.6（700~796 行）精读。

2. **识别 PoC 缺口**：对比 PRD 要求与 PoC 实现，识别 7 项功能缺口（`GAP-01`~`GAP-07`）。

3. **重写 v1.0 源码**：12 个源文件，参数从部分扩展到 75 项（P-01~P-58 全展开），
   校验规则 18 条 → 21 条。

4. **补齐测试基建**：PoC 的 `run_tests.sh` 引用了**未上传**的桩文件，
   即原测试实际不可运行。从零补齐 `test/Arduino.h`、`test/Preferences.h`、
   `test/arduino_stub.cpp`、`test/check.h`，并新增 `test_haptic.cpp`、`test_cli.cpp`、
   `test/build_check.cpp`（原 PoC 显式排除 cli.cpp）。

5. **跑绿测试**：首轮 4 套件 3 失败 → 定位并修复 3 处根因 → 全绿；
   随后补 `build_check`（真实入口 + 双配置），最终 **6 套件 264 断言全绿，0 告警**。

6. **编写文档**：`README.md` + `docs/01`~`docs/05`。

**本轮修复的主要缺陷（详见 `docs/05-CHANGELOG-v1.0.md`）**

| 编号 | 缺陷 | 严重度 |
|---|---|---|
| `FIX-01` | `validateSet()` 先钳制再校验 → 越界值被静默夹进合法区间，`C-09` 等规则永不报出 | 高 |
| `FIX-02` | CLI 把枚举解析器当通用值解析器 → **所有数值型参数都写不进去** | 高 |
| `FIX-09` | 调试通道声称编译期整体移除，实际只隐藏了串口读取，570 行命令实现仍在二进制中 | 中 |
| `FIX-03` | `t feed` / `t auto` 用 `delay()` 阻塞 → 馈入期间状态机停摆，无法观察降落 | 中 |
| `FIX-04` | 首次抚摸被防抖误拦 → 开箱第一下无响应 | 中 |
| `FIX-10` | `config.h` 开关无法命令行覆盖 → 量产配置无法被自动验证 | 中 |
| `FIX-11` | 测试桩缺失，`run_tests.sh` 在 PoC 交付物上实际不可运行 | 中 |
| 其余 | 呼吸链路顺序、无效标签、宏名笔误、X-02 表达式、测试代码 13 处问题 | 低 |

**交付物清单**

| 文件 | 说明 |
|---|---|
| `gruntoy/README.md` | 快速上手（验证 / 接线 / 烧录 / 命令速查 / 调参流程） |
| `gruntoy/docs/01-设计意图与核心目标.md` | 为什么这样设计、目标、非目标、五处「像活物」的代码落点 |
| `gruntoy/docs/02-关键结构与模块边界.md` | 四层架构、模块职责、三层配置、6 态状态机、门控调制、数据流 |
| `gruntoy/docs/03-参数体系与校验规则.md` | 75 项参数分组表、21 条规则全表、三套预设、调参速查 |
| `gruntoy/docs/04-验证与交接说明.md` | 覆盖矩阵、测试边界、M1 判据对应、门禁前必做动作、已知限制 |
| `gruntoy/docs/05-CHANGELOG-v1.0.md` | `GAP` / `FIX` / `NEW` / `OP` 四类变更全记录 |
| `gruntoy/*.h` `*.cpp` `*.ino` | 固件源码 12 个文件 |
| `gruntoy/test/*` | 测试基建 8 个文件 |

**验证结果**

```
 套件         断言通过 断言失败
  test_main            55        0
  test_mood            75        0
  test_haptic          39        0
  test_cli             63        0
  build_debug          16        0
  build_release        16        0
 汇总: 套件 6 通过 / 0 失败
```

量产配置产物 95,648 字节 / 调试配置 113,184 字节（差 17.5 KB，验证 `FIX-09`）。

### 2026-09-21 · 任务二：GitHub 仓库上线 ✅ 已完成

**任务来源**：用户指令「在 github 新建 gruntoy 仓库，把产物都上传」。

**执行过程**

1. **认证链路排查**（三步才找对路）
   - `gh` CLI **未安装**；`git config --global` **为空**（无 `user.name` / `user.email`）。
   - macOS keychain **无** github.com 的 https 凭据（`git credential fill` 会交互式索要用户名，
     非交互环境下直接挂起）。既有仓库 `xingce-assistant` 的 remote 是 SSH。
   - `~/.ssh` 受沙箱拦截（`ls` 报 `Operation not permitted`），但 `ssh -T git@github.com`
     仍可正常认证为 **`licetus`** → SSH 推送通道可用。
   - WorkBuddy GitHub 连接器的 MCP 工具**未注册进 `ToolSearch` 索引**（精确名查询返回 “Not found”），
     但可用 `DeferExecuteTool` **直接调用成功**（`mcp__github__get_me` 返回 `licetus` / id `22375749`）。

2. **建仓受阻与解决**
   - `mcp__github__create_repository` 返回 `403 Resource not accessible by integration`。
     根因：连接器使用的是 **GitHub App 安装令牌**，权限集中不含 `administration: write`，**无法建仓**。
   - 用户确认走 **PAT 路线**：classic token（scope `repo`）经 REST API
     `POST /user/repos` 建仓成功（HTTP 201）。
   - 可见性由用户选定为 **public**；`memorys.md` 由用户确认 **一并入库**。

3. **推送**：远端用 **SSH**（`git@github.com:licetus/gruntoy.git`），
   **PAT 不写入 `.git/config`、不落盘、不回显**，仅用于那一次 API 调用。

4. **验证**：本地 `main` 与 `origin/main` 同为 `7d8bdd6`；远端 31 个文件；
   仓库内 0 处 `ghp_` 残留、`git config` 无令牌；推送后重跑 `bash test/run_tests.sh`
   仍 **6 套件 264 断言全绿 / 0 告警**。

**关键结论（下次直接沿用）**

| 项 | 结论 |
|---|---|
| 仓库地址 | https://github.com/licetus/gruntoy |
| 可见性 / 默认分支 | public / `main` |
| 首个提交 | `7d8bdd6`，31 文件 / 7021 行 |
| 远端协议 | **SSH**，remote 名 `origin` |
| 入库范围 | `gruntoy/` 全部产物 + `memorys.md` |
| 排除内容 | `test/build/`、`.DS_Store`、编辑器目录（见 `.gitignore`） |

**坑位记录（重要）**

- **建仓只能用带 `repo` scope 的 PAT 走 REST API，或由用户在网页手建空仓库**。
  GitHub 连接器的 App 令牌只能读写已有仓库内容，**建不了仓库**。
- 新建仓库**必须单独设置本地 git 身份**（全局配置为空）。本仓库设为
  `licetus` / `22375749+licetus@users.noreply.github.com`。
- 连接器的 GitHub MCP 工具要**用 `DeferExecuteTool` 直接调**，`ToolSearch` 索引不覆盖它们。
- 提交前必须核对 `.gitignore` 生效（本次 31 个文件入库、`test/build/` 下 19 个产物被排除）。

### 2026-09-22 · 任务三：第一个原型产品开发指南 ✅ 已完成

**任务来源**：用户指令「现在我要开发第一个原型产品，给我一份指南」。

**范围与形式（经两次确认）**

| 项 | 用户选择 |
|---|---|
| 覆盖范围 | **M1 完整原型（体感 + 摇尾）** —— 对齐 OPC 总览的 M1 定义，两份 PoC 并行 |
| 交付形式 | **仓库文档 + 可打印记录表** |

**核心判断：这份指南的增量不在复述上游清单，而在「把固件接进流程」**

上游三份清单写于固件不存在之时，其中 **4 项「写代码」任务已被 v1.0 覆盖**：

| 上游清单任务 | 上游做法 | v1.0 之后 |
|---|---|---|
| 体感 Day2 PWM 输出 | 从零写 | `t bare <duty>` |
| 体感 Day4 呼吸包络 | 从零写 | `t breath 1\|2\|3` |
| 体感 Day5 咕噜 25~50Hz | 从零写 | `t purr` / `t sweep` / `t gate` |
| 体感 Day7 心情值状态机 | 从零写 | `t feed` / `t mood` / `t cycle` |

**一句话：上游清单教你造工具，本指南教你用工具。**

**本轮核出的三处事实问题**

1. **PRD §13.2 存在陈旧交叉引用**：原文写「W4 档 2.0Hz 频率上限（**P-31**）」，
   但附录 C 的 P-31 是 `purr_cumulative_limit_ms`，W4 频率正确编号是 **P-47**。
   固件用的是 P-47，正确。**回填 PRD 时须一并订正这一处。**
2. **固件已刻意偏离 PRD 的 P-26**：PRD 写 `[20000,30000,45000,60000]`，固件为
   `[20000,32000,46000,60000]`。原因是原值在心情值 104→105 处单点跳变 10 秒，
   违反 FR-40「不可跳跃切换」（修正后最大跳变 2800ms）。**两个数都是估算，Day5~7 实测后须同时更新固件与 PRD。**
3. **摇尾执行器驱动存在能力缺口（已精确量化）**：`config.h` 的 `TAIL_PWM_FREQ = 50`
   **已经是舵机帧率**，但 `tail.cpp` 的占空比映射是 `50 ± amp×25`（→ 5~15ms 脉宽），
   面向 **H 桥驱动直流电机**；舵机需要 **0.5~2.5ms（2.5~12.5% 占空比）**。
   → 走舵机方案只需改一个常量（`duty = 7.5 ± amp×5`）；走 N20 + H 桥则现有映射直接可用。

**交付物**

| 文件 | 说明 |
|---|---|
| `gruntoy/docs/06-原型开发指南.md` | M1 门禁 7 项证据对照、合并采购清单（¥811~985）、14 天双线日程、体感线 Day0~8 逐日命令、摇尾线 Day1~14、命令速查卡、**M1 常用参数 P 编号↔参数名↔默认值对照表**、体感 12 项 + 摇尾 9 项判据、三项否决性判据、12 条常见坑 |
| `gruntoy/field-forms.html` | 14 张现场记录表，A4 打印优化，402 个可填单元格，浏览器内可直接填写 |

**校验方式（写入前做的机器核对，非目测）**

- HTML：Python `HTMLParser` 验标签配对（全通过）、正则查属性引号笔误（0 处）、
  逐表列数一致性（24 张表全部一致，含 colspan）。
- 文档：脚本提取指南引用的全部参数名 → 与 `params.cpp` META 表 75 项比对（全部命中，
  5 处「未命中」经核实是 PRD 数组型参数名，属正确引用）；13 个 `t` 子命令全部存在于 `cli.cpp`。
- 编号：解析 `params.h` 枚举 + `params.cpp` META，按 `// P-nn` 注释锚点对齐，
  再与 PRD 附录 C 原文逐条核对，生成准确的 P 编号映射表。

**坑位记录**

- **枚举首项形如 `P_X = 0,`**，用 `(P_\w+)\s*,` 之类的正则提取会漏掉它 → 必须允许 `= value`。
- **数组型参数展开后 P 编号会「错位」**：`tail_freq_w1~w4` 全部对应 PRD 的 **P-47**（`tail_freq_hz` 数组），
  不是 P-47/48/49/50。按枚举顺序前向填充编号会得到错误结果——**必须回 PRD 附录 C 核对**。

---

## 5. 下一步待办

> **动手做原型时以 `gruntoy/docs/06-原型开发指南.md` 为准**（14 天日程、逐日命令、判据、记录表）。
> 下表是项目级视角的待办汇总，与指南互为索引。

| 优先级 | 事项 | 依赖 | 归属 |
|---|---|---|---|
| P0 | **采购 M1 物料**（合并清单见 `docs/06` §2.1，¥811~985；最小启动包 ¥420） | 预算 | 采购 |
| P0 | **M0 并行启动**：商标近似检索 + 摇尾机构 FTO 检索（需 2~4 周）+ 认证路径询价 | 无 | 法务 / 代理 |
| P0 | 烧录调试配置，做 L3 真机体感验证：呼吸嗡鸣 / 咕噜猫感 / 摇尾过渡 | 物料到位 | 硬件 + 人工试听 |
| P0 | 首次上电先 `RESET ALL` + `SAVE`（v1.0 schema=2 与 PoC 不兼容） | 无 | 现场 |
| P0 | 用 `t sweep` / `t gate` 扫频，记录各频点主观评分，定稿 `purr_drive_freq_hz` | 上一步 | 现场 |
| P1 | 接入触摸采样，桥接到 `onStroke()`，启用 `P-05` 真实语义（`OP-01`） | 电极装配与阈值标定 | 固件 |
| P1 | 摇尾机构选型，接上后置 `ENABLE_TAIL_OUTPUT=1`；**若走舵机方案需改占空比映射常量**（见 `docs/06` §5.0） | 机构选型结论 | 机构 + 固件 |
| P1 | 电池 ADC 接入，启用 `LOWPOWER` 态（`OP-03`） | 分压电路 | 硬件 + 固件 |
| P1 | **订正 PRD 两处**：§13.2 的 `P-31` → `P-47`；附录 C 的 P-26 与固件对齐 | M1 实测数据 | 文档 |
| P2 | 云端参数下发通道（`OP-04`） | 协议与鉴权方案 | 固件 |
| P2 | NFC 周边联动 | 产品方向确认 | 固件 |
| P2 | 远端仓库加 GitHub Actions：push 时自动跑 `test/run_tests.sh`（对齐 `xingce-assistant` 的 CI 习惯） | 无 | 固件 |

**新对话开工提示**：若继续 gruntoy 项目，先读
`gruntoy/README.md` §2 目录结构 + `gruntoy/docs/04-验证与交接说明.md` §5~§6
（门禁前必做动作与已知限制），再按 §5 表格推进。改动代码后**必须**跑
`bash test/run_tests.sh`，要求 264/264 全绿且 0 编译告警。

**仓库同步约定**：本文件在仓库内有一份副本 `gruntoy/memorys.md`。
更新本文件后，需 `cp memorys.md gruntoy/memorys.md` 并提交推送到
`licetus/gruntoy`，否则远端记录会落后于本地。推送前先 `git status` 确认
`test/build/` 未被纳入。
